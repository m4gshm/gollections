name: Run Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: 1.18rc1-bullseye

    steps:
    - uses: actions/checkout@main

    - name: Run tests
      run: make test
